#!/usr/bin/node
/*
 * This script prints 3 lines using an array of strings and a loop.
 * Each line corresponds to an element in the array.
 */

const mugo = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let faith = 0; faith < mugo.length; faith++) {
  console.log(mugo[faith]);
}
